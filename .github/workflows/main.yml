name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Vue to Github Pages
        uses: xRealNeon/VuePagesAction@1.0.0
        with:
          username: kurzai
          reponame: logic-game
          token: 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
